import "../core/ops.tao"
import "fmt.tao"

#! Utilities for programming with strings.

type Str = [Char]

for B < Show member [Char] of Add B =
    => Output = Str
    => add = fn s, b => s ++ b->show
