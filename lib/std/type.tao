#! Utilities for type-level programming.

import "str.tao"

class Any =
    => type_name : Str

for A member A of Any =
    => type_name =
        let xs : [A] = [] in
        @type_name(xs)

fn type_name_of A < Any =
    _ : A => A.type_name
