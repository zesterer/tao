#! Arithmetic operations.

import "lib/core/ops.tao"

class Zero =
    => zero : Self

member Nat of Zero =
    => zero = 0

class One =
    => one : Self

member Nat of One =
    => one = 1

class Num < Zero + One + Add Self + Mul Self
