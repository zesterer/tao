# Assist in defining recursive functions
let rec = f ->
    f:f
in

# Find the nth element in a list
let nth = rec(nth -> n -> l ->
    if n < 1
    then <: l
    else nth(nth, n - 1, :> l)
) in

0 + nth(0, [1, 2, 3])
