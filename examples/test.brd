def len of List a -> Num = l ->
	if l = []
	then 0
	else 1 + len(:> l)

#def eq_len of List a -> List b -> ? = a -> b -> a:len = b:len

#def main = [1, 2, 3, 4, 5]:len = [true, false, true]:len
#def main = eq_len([1, 2, 3, 4, 5], [true, false, true])

def foo of a -> Num = x -> 0

def bar = x -> y -> foo(x) + foo(y)

def main = bar(0, true)
