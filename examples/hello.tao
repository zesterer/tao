import "../lib/main.tao"

$[ty_debug]
def main : io ~ () = do {
    let sum : [?] = range(0, 10)
        -> map(fn x => do {
            print(x->show)!;
            x + 2
        })
        -> collect;
    # when sum is
    #     | [x] => x!
    #     \ _ => ()
    # ;
    print("Hello, world!" + sum->show)!;
}
